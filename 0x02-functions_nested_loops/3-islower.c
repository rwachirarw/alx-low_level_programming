#include <stdio.h>
#include "main.h"
/**
 *** _islower - 1 if c is lowercase Returns 0 otherwise
 *** Return 1 if c is lowercase Returns 0 otherwise
 ***/

int _islower(int c)
{
	if (c > 90)
		return (1);
	return (0);
}
