#include <stdio.h>
/**
 *main-program start
 *@argc: argument count
 *@argv: argument array
 *Return: interger
 */

int main(int argc, char **argv)
{
	int i = 0, c = 0;

	for (i = 0; i < argc; i++)
	{
		argv[i];
	}
	printf("%d\n", i - 1);
	return (0);
}
