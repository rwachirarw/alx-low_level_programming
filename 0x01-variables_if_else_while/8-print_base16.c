#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *** main - Entry point
 ***
 *** Return: Prints alphabets in lowercase
 ***/
int main(void)
{
int numbers;
for (numbers = 0; numbers <= 15; numbers++)
{
	putchar('16' + numbers);
}
putchar('\n');
return (0);
}
